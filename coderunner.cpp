#include<bits/stdc++.h>
using namespace std;

struct node {
	int data;
	node *left, *right;
	node (int value) {
		data=value;
		left=right=nullptr;
	}
};

node *Build_tree() {
	int rootVal, leftVal, rightVal;
	cin>>rootVal;
}

int main() {
	
	return 0;
}